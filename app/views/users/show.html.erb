<div class="container-fluid text-center show-bg preview-height">    
     <h2 class="text-white text-center">Manage Account:</h2>
     <div class="row text-center border">  
        <div class="col-sm-12 bg-white">
            <%= link_to("Edit", edit_user_path) %>
            <%= link_to "Delete Account", user_path(current_user.id), method: 'delete' %>  
        </div>
    </div>
<%# ------------------------------------------------------------------------------- %>    
    <h2 class="text-white">Manage Posts:</h2>

    <% current_user.posts.each do |p| %> 
        <div class="row text-center border"> 
            <div class="col-sm-2 bg-white no-padding show-height"><img src="<%= p.image.length > 0 ? p.image : default_background_url %>" style="width: 300px; height: auto;"></div> 
            <div class="col-sm-10 bg-white h1 no-padding show-height"
                <p><%= link_to(p.title, post_path(p)) %><br>
                <%= link_to("Edit", edit_post_path(p)) %>
                <%= link_to("Delete", "/posts/#{p.id}", method: "delete")%></p> 
            </div> 
        </div>
    <% end %>   
<%# ------------------------------------------------------------------------------- %>  
</div>